
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    registry-with-eureka: true
    service-url:
      defaultZone: http://localhost:8090/eureka

spring:
  application:
    name: TO-DO-SERVICE
server:
  port: 5001